// CODING CHALLENGES 1 AND 2
/* let markMass = 95;
let markHeight = 1.88;

let johnMass = 85;
let johnHeight = 1.76;

const markBmi = markMass / markHeight ** 2;
const johnBmi = johnMass / (johnHeight * johnHeight);

const markHigherBMI = markBmi > johnBmi; */

/* console.log("Mark's BMI: " + markBmi);
console.log("John's BMI: " + johnBmi);
console.log("Mark's BMI is higher than John's? " + markHigherBMI); */

/* if (markBmi > johnBmi) {
    console.log(`Mark's BMI (${markBmi}) is higher than John's (${johnBmi}).`);
} else {
    console.log(`John's BMI (${johnBmi}) is higher than Marks's (${markBmi}).`);
} */

// CODING CHALLENGE 3
/* const dolphinAverage = (97 + 112 + 101) / 3;
const koalaAverage = (109 + 95 + 106) / 3;

console.log(`Dolphin's average: ${dolphinAverage}`);
console.log(`Koala's average: ${koalaAverage}`);

if (dolphinAverage > koalaAverage && dolphinAverage >= 100) {
    console.log("Dolphins win");
} else if (koalaAverage > dolphinAverage && koalaAverage >= 100) {
    console.log('Koalas win');
} else if (koalaAverage === dolphinAverage && koalaAverage >= 100) {
    console.log("It's a draw");
} else {
    console.log('No one won')
} */

// CODING CHALLENGE 4
/* const bill = 430;
const tip = bill * (bill >= 50 && bill <= 300 ? .15 : .2);
console.log(`The bill was ${bill}, the tip was ${tip}, and the total value was ${bill + tip}`); */
